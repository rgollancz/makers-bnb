
<div class="spaces_container">
  <div class="individual_space_summary">
    <h1>Spaces</h1>
    <div class="space_deets">
      <div class="error_message">
        <% if @booking_failed %>
        Dates unavailable - please select a different date range
        <% end %>
      </div>
      <p>
        <h2><%= @space.name %></h2>
        <%= @space.address %><br>
        <%= @space.description %><br>
        £<%= @space.price %> per night<br><br>
      </p>
      <% if current_user %>
      <form action="/bookings/<%= @space_id %>" method="post">
        <h2>Check-in date</h2>
        <div>
          <input name ="start_date" type="date" data-date-inline-picker="false" data-date-open-on-focus="true" required="true" />
        </div>
        <h2>Check-out date</h2>
        <div>
          <input name ="end_date" type="date" data-date-inline-picker="false" data-date-open-on-focus="true" required="true" />
        </div>
        <br/>
        <div>
          <input type="submit" value="Request to book" />
        </div>
      </form>
      <br>
      <form action="/spaces" method="get">
        <input type="submit" value="View all spaces">
      </form>
    </div>
  </div>
</div>
<% else %>
Sign up to book<br><br>
<form action="/users/sign_up" method="get">
  <input type="submit" value="Sign up">
</form>
<% end %>

<style>
h2 {
  color: #000000;
}
.space_deets {
  background-color: #e6f2ff;
  font-size: 14px;
  font-weight: 400;
  padding: 15px;
  border-radius: 4px;
  display: flex;
  flex-direction: column;
  justify-content: left;
  align-items: left;
}
.individual_space_summary {
  color: black;
  font-size: 14px;
  font-weight: 400;
  padding-left: 20px;
  border-radius: 4px;
  display: flex;
  flex-direction: column;
  justify-content: left;
  align-items: left;
}
.spaces_container {
  display: flex;
  flex-direction: column;
  justify-content: left;
  align-items: center;
  position: inherit;
  justify-content: left;
  padding-left: 20px;
}
</style>
